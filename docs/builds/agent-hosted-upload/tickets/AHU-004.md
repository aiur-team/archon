# AHU-004 — Publication state and strongly consistent store adapter

**Kind:** executable
**Researched at:** dfbd7a7ed2bfb0b2f1742e1d1e2294ab599d1b79
**Requirements:** R5, R7, R8, R9, R10, R12, R13, R14, R15
**Phase:** 2
**Dependencies:** AHU-001
**Complexity:** 3
**Risk:** High: ambiguous completion, concurrent ownership races, private partial content and terminal-record overwrite.

## Outcome

One durable publication record is the authority for approval, ownership, completion and receipt recovery.
Concurrent callers cannot create a second document or overwrite a winning cancellation/completion.
A lost successful write response is resolved by reading back the same record before retry/failure decisions.
Downstream handlers receive a narrow state adapter and immutable fixtures, so they can merge independently.
Thin start/status/cancel HTTP handlers expose these operations; artifact HTTP and browser decision UI belong to consumers.

## Context and evidence

- Read [frozen contracts](../contracts.md) before [authentication research](../research/authentication.md).
- The frozen single-record approach supersedes the research's provisional lease/multi-stage publishing recommendations.
- Baseline netlify/lib/store.mjs:82 restricts legacy document IDs to six hex characters.
- Baseline netlify/lib/store.mjs:174 opens site-wide strong storage.
- Its mutate contract at line 491 explicitly does not supply request-replay idempotency.
- Its write exception handling at line 557 reports unavailable without hosted-operation readback.
- Baseline netlify/lib/access.mjs:550 requires env:DOC_OWNERS provenance and nonempty owner email.
- Baseline access resolveRole at line 1046 can seed ownership from environment email.
- None of these legacy rules is a valid hosted owner binding; use the new independent deployment boundary.
- [Netlify Blobs documentation](https://docs.netlify.com/build/data-and-storage/netlify-blobs/) describes strong consistency and conditional writes.
- Confirm exact @netlify/blobs@11.0.2 return shapes against installed package declarations/source before coding.
- Do not infer conditional delete, object transactions or deploy persistence from an in-memory fake.

## Scope

Consume AHU-001's record validators and create the publication key builder and strongly consistent store adapter.
Implement create-only pending publication and guarded pending/approved/complete/denied/cancelled/expired transitions.
Implement receipt projection and recovery behavior without exposing stored hashes or HTML to status consumers.
Implement bounded ETag conflict retry and explicit readback for ambiguous writes.
Publish immutable fixtures and an adapter for HTTP/private-read consumers; implement frozen start/status/cancel HTTP wrappers.
Document retention limitations and paused-maintenance deletion responsibility.

## Non-goals

No artifact upload HTTP parsing, UTF-8 decoding or content delivery route.
No browser bind/review/decision endpoints or approval page.
No OAuth identity/session code or shared account index.
No second artifact blob, queue, publishing lease, background worker or multi-object commit protocol.
No TTL deletion job or stale-listing delete loop.
No extension of legacy six-hex identifiers, legacy access records or DOC_OWNERS.
No production storage mutation or live provider tests without the later operator gate.

## Existing owner and reuse target

AHU-001 supplies the separate hosted deployment and pinned Blobs dependency.
Use the legacy store's consistency, opaque ETag and bounded retry discipline as reference, not a direct import.
This ticket owns hosted publication state only; auth-state storage remains AHU-003's independent concern.
Keep production modules under hosted/ so a clean hosted install resolves imports before docbuild compilation.
Keep provider-specific behavior in one adapter; HTTP handlers must not call getStore directly.
Use one transition implementation consumed by decision, upload, cancel and status routes.
A fixture can provide a minimal completed HTML example, but it must use the same complete-record validator as production.

## Contract and invariants

The following C2 and C3 blocks are reproduced verbatim from contracts.md.
C3 is included because the adapter must preserve the externally frozen retry and terminal-state meanings.

## C2. Artifact and publication record

`ArtifactDescriptor = { v:1, title:string, contentSha256:lowercaseHex64, contentBytes:integer, artifactFormat:"html" }`.
Title is 1–160 Unicode scalar values, escaped as text in all UI; HTML is 1–2,097,152 bytes, strict UTF-8 with optional UTF-8 BOM, no NUL, and contains an HTML document element.
Accept self-contained single-file HTML, not ZIP, URL fetches, edit manifests, history sidecars, or asset paths. The server does not dereference HTML resources or claim to sanitize arbitrary scripts.
The builder's explicit hosted profile produces inline assets, no Google-font request, no legacy session/comment/edit/realtime code; normal builds are unchanged.

`Publication = { v:1, id:hex32, descriptor:ArtifactDescriptor, state:"pending"|"approved"|"complete"|"denied"|"cancelled"|"expired", agentSecretHash:hex64, browserSecretHash:hex64, userCode:string, createdAt:iso8601, pendingExpiresAt:iso8601, ownerAccountId:null|string, uploadExpiresAt:null|iso8601, completedAt:null|iso8601, receiptExpiresAt:null|iso8601, html:null|string }`.
Server creates a random 128-bit ID; document ID equals publication ID, never a six-hex legacy ID.
Use one strongly consistent site-wide store `archon-hosted-v1`, key `publications/<id>`; never a deploy-scoped or public asset store.
Pending record is create-only; updates use its observed ETag with `onlyIfMatch`, bounded conflict retries, and explicit storage-error handling.
Approval sets immutable owner from browser session plus a ten-minute upload deadline; pending approval expires after fifteen minutes.
Completion replaces the same record atomically with validated HTML and completion metadata. There is no second blob, account index, publishing lease, or multi-record transaction.
Only `complete` records expose bytes, and only to their owner. A complete record and its owner/descriptor/HTML never change in v1.
Each upload CAS attempt rechecks state, owner binding, digest and server deadline. A write already submitted before the deadline may finish afterward; a new attempt after it cannot start. A failed or ambiguous write must be read back before claiming failure or retrying.
Competing upload/cancel/expiry writes cannot override a completed record; completion and cancellation race by CAS and precisely one wins.
Expiry is enforced on every use even if physical cleanup never runs. Keep complete records after receipt expiry; do not delete a committed document as expired auth state.
Netlify provides no verified conditional delete here: v1 does not automatically delete publication records. An operator census and paused-maintenance retention runbook own physical removal; no stale-listing cleanup worker ships.

## C3. HTTP handoff and retry protocol

### Publication adapter (AHU-004 producer)

`hosted/lib/publications.mjs` exports `createPublication(descriptor)`, `readPublication(id)`, `bindPublication({publicationId,browserSecret})`, `reviewPublication({publicationId,browserBinding,principal})`, `decidePublication({publicationId,browserBinding,principal,decision,displayedAccountId})`, `statusPublication({publicationId,agentSecret})`, `cancelPublication({publicationId,agentSecret})`, `completePublication({publicationId,agentSecret,html,contentSha256,contentBytes})`, and `readOwnedPublication({publicationId,principal})`.
create/status/cancel return frozen C3 bodies; create secrets are returned only once. review returns frozen C3 safe review fields.
bind returns server-internal `{publicationId,browserSecretHash}` only after secret verification; AHU-007 stores this behind an opaque HttpOnly browser binding. review/decision accept binding only from its server verifier, never client JSON.
complete returns `{created:boolean,result}` for HTTP 201 versus 200; readOwned returns a complete Publication or not_found. readPublication is server-internal and never an HTTP projection.
status may serve as agent-authorized upload preflight before reading the body; complete revalidates all guards independently. No HTTP handler writes storage or duplicates CAS.
Preserve the optional UTF-8 BOM in html with BOM-preserving decoding and exact re-encoding; the owner's returned bytes must hash to the approved input digest.

### Wire protocol

`POST /api/hosted/publications` accepts only ArtifactDescriptor and returns HTTP 201 `{v:1, publicationId, verificationUriComplete, userCode, agentSecret, expiresAt, intervalSeconds:5}`.
Generate independent 256-bit agent and browser secrets; persist hashes only. Browser URL carries browser secret in fragment, never agentSecret. Trusted first-party bootstrap removes the fragment immediately, exchanges it with the server, and binds the operation in an HttpOnly browser cookie before sign-in. Do not send secrets in query strings or access logs.
User-visible short code is a pairing check only: no short-code lookup endpoint exists. Do not claim an agent-supplied title authenticates a device or Claude identity.
`POST /api/hosted/publications/bind` accepts `{publicationId,browserSecret}` with exact Origin and transient browser CSRF binding and sets pending-browser binding; it does not approve or expose agentSecret.
`GET /api/hosted/publications/<id>/review` requires that binding and an authenticated session; returns descriptor, userCode, current account, and pending/terminal state, never private bearer fields or HTML.
`POST /api/hosted/publications/<id>/decision` accepts `{decision:"approve"|"deny", displayedAccountId}` with browser binding, exact Origin and session CSRF; confirms displayed account equals current session and fixes owner by CAS.
All agent endpoints reject browser Origin or Cookie headers. Public start (`POST /api/hosted/publications`) requires no bearer because it creates the operation secret; status, artifact and cancel authenticate only `Authorization: Bearer <agentSecret>`. No cross-origin browser CORS grants.
`POST /api/hosted/publications/<id>/status` returns `{v:1, state, expiresAt, intervalSeconds:5, result?:{documentId,url,ownerAccountId,contentSha256,contentBytes}}`.
`PUT /api/hosted/publications/<id>/artifact` accepts `Content-Type: text/html; charset=utf-8` and exact artifact bytes. HTTP 201 on first durable completion, HTTP 200 on identical completed retry, same result shape as status. Reject owner fields and mismatched digest/length; never accept a client document ID.
`POST /api/hosted/publications/<id>/cancel` can cancel pending/approved with agent bearer; complete returns its unchanged result, not deletion.
The same operation bearer permits status/cancel before approval and exact-descriptor upload after approval; it never grants browser login or document reads.
Complete result recovery with that bearer lasts twenty-four hours after completion, including when the upload deadline has passed. After that return `receipt_expired`; the owner can still read the document. The client may offer a distinct `checkPublicationUrl` at pinned service origin `/docs/<saved-publicationId>`, labelled Check publication: it is a login/recovery destination, never a success receipt or claim the document exists. Keep expired exit status until authenticated owner verification. Do not automatically start a replacement publication on ambiguous completion.
Lost start response can leave one inert pending record; no document exists. CLI saves request state before reporting success to caller; subsequent status/resume reuses it.
Errors: `{v:1,error:{code, message, retryable}}`, with safe bounded messages. 400 invalid_request; 401 invalid_capability/session_required; 403 approval_required/forbidden/csrf_failed; 404 not_found; 409 descriptor_mismatch/state_conflict; 410 authorization_expired/receipt_expired; 413 artifact_too_large; 415 unsupported_media_type; 429 rate_limited with Retry-After; 503 unavailable/publishing_disabled.
Denial and cancellation are normal terminal states in status, not transport failures. Unknown JSON/state is a protocol error, never success.
All auth, operation, error and private responses: `Cache-Control: private, no-store`, `Referrer-Policy: no-referrer`, `X-Content-Type-Options: nosniff`; all app HTML surfaces (sign-in/callback landing, `/publish/authorize`, `/docs/<id>`) additionally send CSP `frame-ancestors 'none'`. Only the separate renderer may be framed, by the exact configured app origin.

## Refreshable implementation notes

Proposed owned paths:

- hosted/lib/publication-store.mjs: site-wide strong provider adapter and conditional-write/readback handling.
- hosted/lib/publications.mjs: state transitions, secret verification and immutable owner/descriptor enforcement.
- hosted/functions/publications-start.mjs, publications-status.mjs and publications-cancel.mjs: method, header and safe-error wrappers.
- hosted/lib/publication-store.test.mjs, publications.test.mjs and hosted/functions/publications-agent.test.mjs.
- hosted/test/fixtures/publications.mjs: immutable descriptor, pending/approved/complete records and principals.
- hosted/test/helpers/publication-store.mjs: deterministic strong-store double with ETags and fault hooks.
- hosted/docs/publication-adapter.md: signatures, transition table, typed failures and example consumer usage.

Import validateDescriptor, validatePublication, validateResult, validateOrigin, HOSTED_LIMITS and PUBLICATION_STATES from hosted/lib/contracts.mjs as needed; do not duplicate their schemas.
Read validated operator settings through hosted/lib/config.mjs readHostedConfig(env).
Before merge, document the actual exports in the adapter document and use them in the immutable consumer examples.
Use the exact frozen C3 adapter exports; do not accept arbitrary mutation callbacks from handlers.
A complete transition receives bytes already validated by the artifact layer plus verified digest/length facts.
It must still enforce the stored descriptor and owner/state/deadline invariants before each CAS.
The artifact HTTP layer owns strict UTF-8 and HTML syntax acceptance; this layer owns durable completion semantics.
Record validation must distinguish JSON-safe strings from ill-formed record envelopes and unknown state names.
Use a fixed bounded attempt limit, proposed six attempts matching existing discipline; document the chosen internal value.
On resolved modified:false, read again and re-evaluate the entire transition against fresh state/time.
On thrown or malformed provider write response, read back before deciding whether it committed or remains retryable.
A matching complete readback returns the committed receipt, not a retry that allocates a new ID.
An observed noncomplete record can support a bounded retry only after its guards and deadline are rechecked.
A mismatched terminal record yields its explicit terminal semantics or state_conflict; never overwrite it.
Initial completion and identical completed retry need distinct outcome metadata for AHU-008's 201/200 mapping.
Use provider dependencies injected into factories; production always selects site-wide strong consistency.
Do not use process memory as cross-request authority or conditional delete as revocation.
Record expiry is logical, evaluated at use; maintenance can inspect stale records later without changing access behavior.

## Acceptance and verification

A rejected upload does not consume approval or mutate descriptor/owner; retry of the originally approved exact bytes remains possible until expiry. Correcting bytes changes the descriptor and requires a fresh explicit start/approval. Do not implement cross-authorization idempotency: operation identity is this server-issued publication ID plus its saved capability.

### Agent gate

Wire every owned test by literal path into .github/workflows/check.yml in this PR.
Required command: node --test hosted/lib/publication-store.test.mjs hosted/lib/publications.test.mjs hosted/functions/publications-agent.test.mjs.
Run node scripts/check-test-inventory.mjs and the AHU-001 clean hosted module-load check.
Tests use explicit injected stores and clock; no Netlify project, real global state or default home path is read.
Verify adapter rejects malformed hashes, legacy IDs and bad state through foundation validators; handler tests reject Origin/Cookie, bad methods, wrong bearer and invalid descriptors.
Verify create-only collision preserves original record and never reports the colliding request as its creator.
Race two approving accounts with the same observed ETag; exactly one immutable owner must survive.
Race approve against deny/expire; terminal state cannot be revived.
Race completion against cancel; assert one permitted terminal result and zero partial readable states.
Race two identical completions; assert one completed record and same receipt/document ID.
Retry with different descriptor facts or owner; the record cannot change.
Simulate write committed then response thrown; readback returns complete without duplicating or reporting definitive failure.
Simulate write not committed then response thrown; readback precedes retry and rechecks the server deadline.
Simulate ambiguous write plus failed readback; assert retryable unavailable and retained uncertainty.
Advance time across the deadline between conflict attempts; no new CAS submission may begin after expiry.
Allow an already-submitted pre-deadline CAS to finish after it; the resulting complete record must be retained.
Verify completed receipt lookup works after upload expiry until exactly the receipt deadline.
After receipt expiry, assert owner-read state still contains the complete HTML and owner.
Disable cleanup entirely in tests; pending/upload logical expiry must continue to hold.
Assert status/review projections omit secret hashes and HTML, including terminal and unavailable paths.

### At-merge

All literal tests and existing CI checks pass on the ticket head with a clean hosted install.
Document immutable fixture imports and adapter signatures for AHU-007 and AHU-008 in this PR.
Every new test must fail when its specific production guard is removed in an isolated clean worktree.
Mutation examples: remove onlyIfMatch, bypass owner comparison, skip readback, reuse stale time, expire complete records.
Assert effects and winning stored data, not merely constant schema values or chosen error strings.
Restore each production hunk and rerun; PR body includes exact mutation commands and per-test outcomes.
A fake that bypasses conditional writes or cannot model write-then-throw does not cover these invariants.
Do not claim real provider concurrency semantics from fixtures; mark that evidence as deterministic adapter verification.

### Human

No human credentials are needed for the deterministic ticket gate.
The live capstone must race real conditional writes on a controlled Netlify site and verify lost-response recovery.
An operator-owned retention census/runbook must explain that expiry does not physically remove records.
A live failure of conditional semantics blocks rollout; it does not authorize weakening the frozen single-record contract.

## Failure, security, migration, and accessibility cases

Malformed stored JSON, missing ETag, invalid provider result and read failure have explicit safe unavailable handling.
Unknown state and unknown schema version are never coerced into pending or complete.
Secret comparison and caller-controlled IDs are validated before expensive data access where feasible.
The same agent capability is bounded by state and deadlines; it never grants account or document-read authority.
Pending metadata cannot accidentally contain uploaded private bytes.
Concurrent cancellation is not deletion, and a completed record remains durable after a cancelled caller retries.
Hash/digest mismatch cannot attach replacement bytes to an already approved descriptor.
No opaque provider detail, HTML, bearer or owner snapshot is logged during retries.
Complete record retention preserves source bytes, including optional BOM, for the content endpoint's integrity checks.
There is no legacy migration; separate IDs/store/deployment prevent accidental lookup through old seed rules.
This backend ticket introduces no user interface; safe state/error shapes enable accessible consumer messaging.
Avoid transport-level success for a missing record or unavailable backing store.

## Surfaces

New hosted library modules, deterministic fixtures, adapter documentation and additive CI entries.
Frozen start/status/cancel endpoints are added; start observes HOSTED_PUBLISH_ENABLED while status receipt recovery remains available.
The archon-hosted-v1 store and publications/<id> key namespace are the only publication persistence surface.

## Sibling boundaries and open gates

AHU-001 precedes this ticket; AHU-003 may implement identity independently in phase 2.
AHU-007 owns bind/review/decision HTTP and UI; it consumes these transitions and cannot add another CAS loop.
AHU-008 owns artifact upload HTTP, byte validation and Content-Type behavior; it may call statusPublication as authenticated preflight.
AHU-011 adds platform rateLimit configuration to this ticket's start/status files, independently of the artifact handler.
The private-read consumer checks this record's owner/complete state before returning any content.
Consumer tests may use immutable fixtures; they may not invent a divergent Publication shape.
Coordinate additive CI conflicts by retaining every literal sibling test path.
Any change to C2/C3 requires coordinated contract/consumer edits and a new planning version before dispatch.
No live provider credentials, production writes, cleanup job or implementation lease is an implied task dependency.
