# AHU-012 — Exercise the real publishing chain and isolation boundaries

**Kind:** executable

**Researched at:** `dfbd7a7ed2bfb0b2f1742e1d1e2294ab599d1b79`

**Requirements:** R1, R2, R3, R4, R5, R6, R7, R8, R9, R10, R11, R12, R13, R14, R15

**Phase:** 4

**Complexity:** c3

**Risk:** high; independently passing producer tests do not prove ownership, authorization and rendering stay connected.

**Dependencies:** AHU-007, AHU-008, AHU-009, AHU-010, AHU-011.

## Outcome

A repeatable CI integration gate drives the installed client, actual hosted routes, actual browser approval, durable storage producer and configured renderer as one system. It catches mismatches between producers and demonstrates owner-only read, resumable publication and active-content isolation locally. Live GitHub and platform guarantees remain the separate capstone.

## Context and evidence

Baseline CI `.github/workflows/check.yml` explicitly names every test and guards deploy modules before compilation. Existing `scripts/test-p4-k-browser.mjs` and `scripts/test-p4-l-browser.mjs` show local browser fixtures and rendered assertions; they cover legacy identity/access, not the new hosted application.
AHU-006 can prove a client against a protocol fixture; AHU-007/AHU-008/AHU-009 can prove individual handlers. Those tests cannot establish that the approval, upload receipt and reader refer to the same real record/account.
AHU-010's package proof deliberately uses a protocol fixture. This ticket replaces that successful service fixture with the actual producer handlers, browser pages and persistence modules, while keeping only upstream identity controlled.

## Scope

- Add a repeatable local integration runner and focused malicious/benign HTML fixtures; exercise actual produced app and static renderer over supported local origins.
- Pack/install the real publishing binary outside the repository, build with the real hosted profile, start publication, approve through the real browser UI and resume via a separate client process.
- Connect client→server, browser approval→upload, upload→owner reader, and deployment headers/origin configuration→renderer. Assert actual durable record and rendered behavior.
- Use a deterministic GitHub OAuth upstream fixture for local authorization; exercise real state/PKCE/cookie/session validation and real callback handler. Inject storage failures at a narrow boundary around the actual storage producer for error/race tests.
- Wire the runner into CI and record local evidence without private HTML, tokens, browser secrets, cookies or provider response dumps.
- Fix scoped cross-producer defects exposed by the gate with responsible producer coordination; do not ship a generic test framework as the outcome.

## Non-goals

No mock-only “end-to-end” success, fixture authentication that injects a principal directly into handlers, browser bypass using a direct approval-store write, replacement success handlers, test-only owner bypass, broad test infrastructure rewrite, live OAuth/Netlify/signup, public launch, or mock closeout of AHU-013.
Do not use this capstone to defer producer tests or wire earlier unwired tests after the fact.

## Existing owner and reuse target

Existing: `.github/workflows/check.yml`, `scripts/check-test-inventory.mjs`, `scripts/lib/temp-roots.mjs`, browser runner patterns in `scripts/test-p4-k-browser.mjs`/`test-p4-l-browser.mjs`.
New relative to baseline, produced by dependencies: `hosted/`, `renderer/`, `templates/docbuild/src/publish-cli.ts`, canonical packaged `skills/archon-doc/SKILL.md`.
New/proposed in this ticket: `scripts/test-hosted-integration.mjs`, `scripts/fixtures/hosted/benign.html`, `scripts/fixtures/hosted/hostile.html` and narrowly needed fixture helpers under `scripts/fixtures/hosted/`.
Discover dependency-owned exact route and test bootstrap filenames after merge; invoke them, do not implement shadow equivalents.

## Contract and invariants

The following frozen contract blocks are reproduced verbatim.

## C3. HTTP handoff and retry protocol

### Publication adapter (AHU-004 producer)

`hosted/lib/publications.mjs` exports `createPublication(descriptor)`, `readPublication(id)`, `bindPublication({publicationId,browserSecret})`, `reviewPublication({publicationId,browserBinding,principal})`, `decidePublication({publicationId,browserBinding,principal,decision,displayedAccountId})`, `statusPublication({publicationId,agentSecret})`, `cancelPublication({publicationId,agentSecret})`, `completePublication({publicationId,agentSecret,html,contentSha256,contentBytes})`, and `readOwnedPublication({publicationId,principal})`.
create/status/cancel return frozen C3 bodies; create secrets are returned only once. review returns frozen C3 safe review fields.
bind returns server-internal `{publicationId,browserSecretHash}` only after secret verification; AHU-007 stores this behind an opaque HttpOnly browser binding. review/decision accept binding only from its server verifier, never client JSON.
complete returns `{created:boolean,result}` for HTTP 201 versus 200; readOwned returns a complete Publication or not_found. readPublication is server-internal and never an HTTP projection.
status may serve as agent-authorized upload preflight before reading the body; complete revalidates all guards independently. No HTTP handler writes storage or duplicates CAS.
Preserve the optional UTF-8 BOM in html with BOM-preserving decoding and exact re-encoding; the owner's returned bytes must hash to the approved input digest.

### Wire protocol

`POST /api/hosted/publications` accepts only ArtifactDescriptor and returns HTTP 201 `{v:1, publicationId, verificationUriComplete, userCode, agentSecret, expiresAt, intervalSeconds:5}`.
Generate independent 256-bit agent and browser secrets; persist hashes only. Browser URL carries browser secret in fragment, never agentSecret. Trusted first-party bootstrap removes the fragment immediately, exchanges it with the server, and binds the operation in an HttpOnly browser cookie before sign-in. Do not send secrets in query strings or access logs.
User-visible short code is a pairing check only: no short-code lookup endpoint exists. Do not claim an agent-supplied title authenticates a device or Claude identity.
`POST /api/hosted/publications/bind` accepts `{publicationId,browserSecret}` with exact Origin and transient browser CSRF binding and sets pending-browser binding; it does not approve or expose agentSecret.
`GET /api/hosted/publications/<id>/review` requires that binding and an authenticated session; returns descriptor, userCode, current account, and pending/terminal state, never private bearer fields or HTML.
`POST /api/hosted/publications/<id>/decision` accepts `{decision:"approve"|"deny", displayedAccountId}` with browser binding, exact Origin and session CSRF; confirms displayed account equals current session and fixes owner by CAS.
All agent endpoints reject browser Origin or Cookie headers. Public start (`POST /api/hosted/publications`) requires no bearer because it creates the operation secret; status, artifact and cancel authenticate only `Authorization: Bearer <agentSecret>`. No cross-origin browser CORS grants.
`POST /api/hosted/publications/<id>/status` returns `{v:1, state, expiresAt, intervalSeconds:5, result?:{documentId,url,ownerAccountId,contentSha256,contentBytes}}`.
`PUT /api/hosted/publications/<id>/artifact` accepts `Content-Type: text/html; charset=utf-8` and exact artifact bytes. HTTP 201 on first durable completion, HTTP 200 on identical completed retry, same result shape as status. Reject owner fields and mismatched digest/length; never accept a client document ID.
`POST /api/hosted/publications/<id>/cancel` can cancel pending/approved with agent bearer; complete returns its unchanged result, not deletion.
The same operation bearer permits status/cancel before approval and exact-descriptor upload after approval; it never grants browser login or document reads.
Complete result recovery with that bearer lasts twenty-four hours after completion, including when the upload deadline has passed. After that return `receipt_expired`; the owner can still read the document. The client may offer a distinct `checkPublicationUrl` at pinned service origin `/docs/<saved-publicationId>`, labelled Check publication: it is a login/recovery destination, never a success receipt or claim the document exists. Keep expired exit status until authenticated owner verification. Do not automatically start a replacement publication on ambiguous completion.
Lost start response can leave one inert pending record; no document exists. CLI saves request state before reporting success to caller; subsequent status/resume reuses it.
Errors: `{v:1,error:{code, message, retryable}}`, with safe bounded messages. 400 invalid_request; 401 invalid_capability/session_required; 403 approval_required/forbidden/csrf_failed; 404 not_found; 409 descriptor_mismatch/state_conflict; 410 authorization_expired/receipt_expired; 413 artifact_too_large; 415 unsupported_media_type; 429 rate_limited with Retry-After; 503 unavailable/publishing_disabled.
Denial and cancellation are normal terminal states in status, not transport failures. Unknown JSON/state is a protocol error, never success.
All auth, operation, error and private responses: `Cache-Control: private, no-store`, `Referrer-Policy: no-referrer`, `X-Content-Type-Options: nosniff`; all app HTML surfaces (sign-in/callback landing, `/publish/authorize`, `/docs/<id>`) additionally send CSP `frame-ancestors 'none'`. Only the separate renderer may be framed, by the exact configured app origin.

## C4. Private read and isolated rendering

`GET /docs/<id>` is a trusted viewer shell; signed-out requests go to a fixed local sign-in flow with a server-validated ID; other users and missing IDs get indistinguishable not-found views. Never include title/HTML before owner authorization.
`GET /api/hosted/docs/<id>` requires browser owner session and complete state, returns `{v:1,documentId,title,ownerAccountId,contentSha256,contentBytes,createdAt}`.
`GET /api/hosted/docs/<id>/content` repeats owner/complete checks and returns UTF-8 bytes as `application/octet-stream`, `Content-Disposition: attachment; filename="archon-document.html"`, nosniff, private no-store. No public Blobs URL, CDN copy, redirect bearer, or downloadable asset path.
Trusted viewer fetches content same-origin, then sends `{type:"archon:render",v:1,html}` ONLY to configured renderer origin and exact renderer frame window after its `{type:"archon:ready",v:1}` message.
Renderer accepts the message only from exact configured app origin and `event.source === parent`. It places HTML in an inner iframe with `sandbox="allow-scripts"`, never `allow-same-origin`, top navigation, popups, forms, downloads, or credentialless fallback claims.
AHU-005 owns a trusted srcdoc prelude for fragment-only links: prevent native base-URL navigation without stopping authored click listeners, resolve the target and scroll after those listeners, and preserve original href attributes and raw stored bytes. Do not require History API or parent navigation authority on the opaque document; missing targets stay in the current artifact. AHU-012 tests actual packaged navigation and verifies the renderer never replaces the artifact.
The outer renderer is cross-site and cookie-free. If sandboxed it needs `allow-scripts allow-same-origin` for exact origin messaging; it hosts only operator-owned static code. It never receives document URL/ID, session, CSRF, owner data or bearer.
Renderer HTTP CSP bounds scripts/styles/frames; inner srcdoc adds an early policy allowing inline script/style, data/blob image media as needed, and denying connect, forms, object, base and remote subresources. No postMessage from artifact can authorize another fetch or resize/navigate trusted UI.
Sandbox prevents account-origin access, not every possible self-navigation or all content exfiltration by malicious HTML; never advertise network-proof or end-to-end encryption. Use a separate registrable site to keep renderer cross-site for SameSite cookies. Application mutation endpoints still enforce Origin/CSRF independently.
Trusted shell exposes title, owner identity, sign-out, loading, failed renderer and unavailable states; the artifact may not impersonate those regions. Keyboard focus, frame title and screen-reader errors are required.

## C6. Operations and acceptance

`HOSTED_APP_ORIGIN`, `HOSTED_RENDER_ORIGIN`, `GITHUB_CLIENT_ID`, `GITHUB_CLIENT_SECRET`, `HOSTED_PUBLISH_ENABLED` are operator configuration; secrets never enter bundles or git. Unset/false publish-enabled refuses new start/upload with 503 while existing private reads and completed receipt recovery remain available.
Use Netlify platform per-IP limits on public start and agent status routes where supported (initial pilot targets 10 starts/minute/IP and 30 status requests/minute/IP); configure exact route ownership and test effective deploy headers/config. This is delayed best-effort mitigation, not a hard global or account quota. Client five-second polling is pacing, not a security boundary.
No billed provider signup or production deployment is authorized by this plan. Operator must supply two HTTPS origins/sites, exact OAuth registration/callback, test accounts, credentials, pilot traffic/budget envelope and retention responsibility before live acceptance. Public unrestricted signup rollout remains gated until abuse/cost exposure is accepted; no database or billing system is smuggled into v1.
Local integration must exercise real handlers and the packaged CLI with a deterministic OAuth provider fixture and real browser. Live capstone separately proves GitHub callback/scopes, deploy routing/headers, conditional-write race semantics, owner/other-account read isolation and lost-response receipt recovery. Fixture proof cannot close the live capstone.

## Refreshable implementation notes

Select the simplest supported local launch path from AHU-001/AHU-011. All success routes and store functions are actual production producers. A supported local Netlify Blobs runtime/emulator is acceptable for local persistence semantics; a hand-written fake `Publication` map replacing the storage module is not.
The actual remote Netlify conditional-write behavior remains AHU-013 proof because local persistence cannot establish provider production semantics. If the local environment cannot execute the real store producer with supported local storage, report that concrete gate and fix the supported launch surface rather than replacing it with passing fixture handlers.
Use local HTTPS/test hostnames where practical so Secure/SameSite cookies and cross-site browser behavior are exercised. Any explicit loopback test mode must be narrow and absent from production acceptance claims. Two ports alone are different origins but not necessarily different registrable sites; record that distinction.
Use a real browser for approval and reading. Poll via installed CLI exactly as an agent would. A proxy may drop a committed upload response to test receipt recovery without replacing the server's upload route.
Prefer bounded waits tied to DOM/state conditions. Capture concise outcomes/screenshots with sanitized account identities; keep artifacts in private temp paths, preserve failure evidence safely, and clean only resources the runner created.

## Acceptance and verification

Test the actual packaged document's section navigation inside the real srcdoc renderer, including authored click listeners, original href preservation and unavailable targets. Also reconnect expired-receipt Check publication with owner-only navigation and verify the real approval/viewer pages reject framing.

### Agent gate

The following are minimum behavioral regression matrices, not a checklist of mocked return values.

1. **Whole happy path:** clean-installed client builds/starts; browser sees correct title, bytes, pairing code and signed-in account, approves through actual UI; separate client resume uploads; server stored owner equals canonical numeric GitHub identity; receipt URL opens owner shell and real artifact; source HTML remains unchanged.
2. **Auth/account binding:** switch between two fixture identities during review; approving a stale displayed identity fails. Replay OAuth state/binding, absent/wrong CSRF or wrong Origin does not fix an owner. GET review/GET callback behavior cannot silently approve.
3. **Upload/receipt binding:** client-provided owner fields or changed digest/size cannot create altered ownership/content. Drop a response after actual durable completion, then resume after upload deadline but within receipt window: same document ID/owner/digest/bytes, one publication, no second start.
4. **Owner read and enumeration:** signed-out, second-account and missing-ID shell/metadata/content cases disclose no private title or bytes; private content is attachment/octet-stream with no-store/nosniff and no public asset URL. A second account using the first owner's known URL still fails.
5. **Storage and race failure:** inject before-write failure, ambiguous after-write failure, CAS conflict and concurrent cancel/upload around real storage operations. Only one terminal record wins; no partial HTML becomes readable; unavailable service never returns a success receipt. Completed cancellation cannot erase the document.
6. **Rendered isolation:** load benign theme/navigation content and operate it. Hostile content attempts parent DOM access, account fetch/mutation, forged ready/render messages, wrong-source messages, forms/popups/top navigation and remote subresources. Assert account authority/outer shell remain protected and check actual blocked network outcomes. Do not assert an absolute no-exfiltration claim forbidden by C4.
7. **Deployment/renderer connection:** real configured origin/headers are applied; viewer sends content only after exact-source ready; wrong renderer origin/source or missing renderer produces a readable failure state without sending bytes. No bearer/cookie/CSRF/owner ID is in the renderer message.
8. **Operations:** disabling publication refuses new starts/uploads while owner reads and completed receipt recovery still function. Enforced payload limits, no-store on error paths and safe logs hold in the actual routed service. Local evidence distinguishes configured platform rate limits from live efficacy.
9. **Accessibility:** keyboard reaches approval/deny and viewer sign-out; title/current account and failure state are visible outside untrusted frame, frame has a meaningful accessible title, and loading/error announcements are exposed to assistive technology.
10. Add `node scripts/test-hosted-integration.mjs` literally to CI in this PR. Run inventory and relevant existing producer checks; no credentials are required in untrusted pull-request CI.
11. Mutation-test new regression ownership in an isolated worktree: remove owner equality check, weaken exact-message source check, skip upload descriptor recheck, and replace ambiguous-write readback with blind failure/retry one at a time. Require specific new integration assertions to fail, restore and pass. Report exact commands and intended-only dirty-tree evidence; never mutate the live operator checkout.

### At-merge

The runner invokes actual merged producers and installed package, not copies of endpoint logic. All regressions run through CI with private temporary state and deterministic provider fixture. Existing self-hosted root gates remain green.
Record local launch topology, browser version, storage environment and explicit limits of local evidence in a durable short report under `docs/builds/agent-hosted-upload/evidence/` (new/proposed generated evidence path). Do not label the feature live accepted.

### Human

Review one local captured flow and its sanitized evidence: account confirmation, approved upload receipt, artifact interactions and non-owner denial. AHU-013 remains open until operator-authorized real GitHub/platform proof succeeds.

## Failure, security, migration, and accessibility cases

A fixture provider outage must reach a truthful unavailable UI; a fixture token with unexpected granted scope must fail through actual auth handling.
Leaked or malformed error data must not enter CLI/browsers/logs. Validate browser-secret fragment removal before subsequent navigation/requests.
Expired/cancelled requests leave local source intact and do not regain upload authority. Receipt expiry leaves owner read intact.
Explicitly test that legacy `DOC_OWNERS`, org defaults and `PUBLIC_DEFAULT_ROLE` values cannot widen new hosted reads. No legacy records migrate.
A frame that cannot load or receive content must expose a usable trusted error; hostile artifact content never supplies trusted account/error UI.

## Surfaces

New integration runner, fixtures, explicit CI step and sanitized evidence report. This ticket may make narrowly scoped changes to dependency-owned modules only to fix demonstrated cross-producer defects and with matching regression ownership.

## Sibling boundaries and open gates

AHU-007 supplies browser approval, AHU-008 upload, AHU-009 owner-only private read/trusted viewer consuming AHU-005's isolated renderer, AHU-010 installed package/skill, AHU-011 deployment/operations. Preserve each boundary rather than “making tests green” through shared hidden bypasses.
AHU-013 alone proves live GitHub callback/scopes, two deployed registrable sites, provider CAS behavior, live headers/rate configuration and external package availability.
If a producer's supported local launch or store interface is missing, report the exact missing surface and repair that in scope; passing a standalone fake service is not completion.
