# AHU-013 — Prove the live hosted publishing pilot

**Kind:** capstone

**Researched at:** `dfbd7a7ed2bfb0b2f1742e1d1e2294ab599d1b79`

**Requirements:** R1, R2, R3, R4, R5, R6, R7, R8, R9, R10, R11, R12, R13, R14, R15

**Phase:** 5

**Complexity:** c2

**Risk:** high operationally; live account authentication, hosted private content and potentially billed infrastructure.

**Dependencies:** AHU-012.

## Outcome

The operator has dated, reproducible evidence that a publicly installable package and its instructions can publish a private owned document through real GitHub sign-in, real deployed service storage and the deployed cross-site renderer. Missing operator prerequisites or failed live guarantees leave this capstone open; local mocks cannot close it.

## Context and evidence

AHU-012 composes real handlers locally with a deterministic GitHub fixture. It cannot prove GitHub's registered callback/actual granted scopes, Netlify production routing/CAS, two real HTTPS registrable sites, or installation from an externally available release.
Baseline `scripts/connect.test.mjs` and `scripts/test-p4-k-hosted.mjs` show the repository's separation between hermetic tests and operator-credential hosted acceptance, including evidence and cleanup. Their provider-account setup is legacy functionality, not authorization to create the new deployments.
The source package is `@aiur-team/docbuild@0.0.1` at the pin; actual release containing AHU-006/AHU-010 must be identified and installed afresh.

## Scope

- Prepare a bounded live acceptance runbook and, where useful, a small credential-safe runner that invokes actual released client/service flows and records sanitized evidence.
- Verify every operator prerequisite before any provider mutation or private-content upload.
- Drive genuine GitHub OAuth browser sign-in/approval and the installed CLI upload, real storage commit/retry/race behavior, owner/second-account denial, deployed renderer isolation and production headers.
- Establish the exact tested package version/integrity, source revision, two site origins, callback registration, runtime deployment revisions and operator-approved pilot budget/enablement.
- Record pass/fail/blocked for each required live guarantee and the cleanup/retention disposition of only the generated test resources.
- Feed genuine defects back into their producer ticket or focused follow-up with reproducible evidence. Close only when required live assertions pass.

## Non-goals

No autonomous billed signup, production deployment, OAuth registration, DNS change, package publication or unrestricted rollout under the authority of this planning document alone.
No feature implementation beyond narrowly needed acceptance tooling, no new delete API, no simulated live success, no public customer-document upload, no large load test, and no test that accesses unrelated users/data.
A shipped runbook or passing fixture is not the capstone outcome.

## Existing owner and reuse target

Existing: `scripts/connect.test.mjs`, `scripts/test-p4-k-hosted.mjs`, `scripts/lib/temp-roots.mjs`, `.github/workflows/check.yml` for gated live-test patterns.
New relative to baseline, produced by prerequisites: all `hosted/`, `renderer/`, publisher and packaged skill surfaces; AHU-011 operator configuration/runbook.
New/proposed capstone-owned: `scripts/test-hosted-live.mjs` if repeatable automation materially helps, `docs/builds/agent-hosted-upload/live-acceptance.md` runbook, and a dated sanitized report under `docs/builds/agent-hosted-upload/evidence/`.
Reuse AHU-012's benign/hostile artifact fixtures without forking successful server or renderer logic.

## Contract and invariants

The following frozen contract blocks are reproduced verbatim.

## C1. Deployment and identity boundary

The new `hosted/` application is a separate Netlify deployment with its own `package.json`, lockfile, `netlify.toml`, `functions/`, `lib/`, and `public/` tree.
It must not load the legacy all-path edge gate, Netlify Identity, `DOC_OWNERS`, organization defaults, or `PUBLIC_DEFAULT_ROLE`.
The existing root deployment and `scripts/connect.mjs` remain unchanged.
Use Node 22 and pinned `@netlify/blobs@11.0.2`; server imports must resolve in a clean install before any builder compilation.
`renderer/` is a second, static-only deployment on a different origin and registrable site, with no account cookies, credentials, private bytes at rest, or functions.
Production requires HTTPS, exact configured origins, and no wildcard callback or CORS origins.

`HostedPrincipal = { accountId: "gh_" + decimalGitHubId, provider: "github.com", providerUserId: decimalGitHubId, login: string }`.
GitHub numeric ID is canonical; username/email are not ownership keys. Do not request email, repository, organization, or installation permissions.
Dedicated OAuth app, empty scope, fixed GitHub endpoints, server code exchange with client secret, S256 PKCE, single-use state bound to an HttpOnly transient browser cookie.
Reject unexpected granted scopes. Fetch `/user`, validate identity, then discard all GitHub access/refresh tokens.
Browser session is an opaque 256-bit random token; persist its hash with principal and a seven-day absolute expiry.
AHU-003 owns strongly consistent private session records in `archon-hosted-v1` at `sessions/<tokenHash>` and transient auth records under `auth/`. Protected logout and callback rotation durably revoke the old session server-side before claiming success; clearing a cookie alone is insufficient. Revocation uses guarded state updates and ambiguous-write readback, never a public or deploy-scoped store. A revoked token fails on every subsequent identity read.
Transient OAuth-state, pre-login CSRF and publication-binding cookies use distinct `__Host-` names with Secure, HttpOnly, SameSite=Lax, Path=/, no Domain, and at most fifteen-minute TTL. Consume OAuth state once; preserve publication binding through account switching until decision or expiry, then clear it. Do not consume all three cookies on the first bootstrap request.
Cookie: `__Host-archon_session; Secure; HttpOnly; SameSite=Lax; Path=/`; no Domain attribute.
`identifyHosted(request)` returns principal or null; backing-store failure throws an unavailable error, never null.
`requireBrowserMutation(request)` verifies the exact configured Origin and a session-bound CSRF token. The pre-login start form uses a separate transient CSRF binding, not a required authenticated session.
GET never approves publication or logs out. OAuth callback consumes state once and rotates session; redirect destinations are only `/publish/authorize` with server-bound operation or `/docs/<id>`.
The different-account action revokes the Archon session by protected POST, preserves the pending browser binding, then starts a fresh browser-bound GitHub authorization with `prompt=select_account`; do not imply Archon logout logs out GitHub.
Routes: `POST /api/hosted/auth/github/start`, `GET /api/hosted/auth/github/callback`, `GET /api/hosted/session`, `POST /api/hosted/auth/logout`.
Session endpoint returns `{v:1, authenticated:false}` or `{v:1, authenticated:true, accountId, login, csrfToken}`; token is browser-only and must never reach artifact or agent output.

## C4. Private read and isolated rendering

`GET /docs/<id>` is a trusted viewer shell; signed-out requests go to a fixed local sign-in flow with a server-validated ID; other users and missing IDs get indistinguishable not-found views. Never include title/HTML before owner authorization.
`GET /api/hosted/docs/<id>` requires browser owner session and complete state, returns `{v:1,documentId,title,ownerAccountId,contentSha256,contentBytes,createdAt}`.
`GET /api/hosted/docs/<id>/content` repeats owner/complete checks and returns UTF-8 bytes as `application/octet-stream`, `Content-Disposition: attachment; filename="archon-document.html"`, nosniff, private no-store. No public Blobs URL, CDN copy, redirect bearer, or downloadable asset path.
Trusted viewer fetches content same-origin, then sends `{type:"archon:render",v:1,html}` ONLY to configured renderer origin and exact renderer frame window after its `{type:"archon:ready",v:1}` message.
Renderer accepts the message only from exact configured app origin and `event.source === parent`. It places HTML in an inner iframe with `sandbox="allow-scripts"`, never `allow-same-origin`, top navigation, popups, forms, downloads, or credentialless fallback claims.
AHU-005 owns a trusted srcdoc prelude for fragment-only links: prevent native base-URL navigation without stopping authored click listeners, resolve the target and scroll after those listeners, and preserve original href attributes and raw stored bytes. Do not require History API or parent navigation authority on the opaque document; missing targets stay in the current artifact. AHU-012 tests actual packaged navigation and verifies the renderer never replaces the artifact.
The outer renderer is cross-site and cookie-free. If sandboxed it needs `allow-scripts allow-same-origin` for exact origin messaging; it hosts only operator-owned static code. It never receives document URL/ID, session, CSRF, owner data or bearer.
Renderer HTTP CSP bounds scripts/styles/frames; inner srcdoc adds an early policy allowing inline script/style, data/blob image media as needed, and denying connect, forms, object, base and remote subresources. No postMessage from artifact can authorize another fetch or resize/navigate trusted UI.
Sandbox prevents account-origin access, not every possible self-navigation or all content exfiltration by malicious HTML; never advertise network-proof or end-to-end encryption. Use a separate registrable site to keep renderer cross-site for SameSite cookies. Application mutation endpoints still enforce Origin/CSRF independently.
Trusted shell exposes title, owner identity, sign-out, loading, failed renderer and unavailable states; the artifact may not impersonate those regions. Keyboard focus, frame title and screen-reader errors are required.

## C6. Operations and acceptance

`HOSTED_APP_ORIGIN`, `HOSTED_RENDER_ORIGIN`, `GITHUB_CLIENT_ID`, `GITHUB_CLIENT_SECRET`, `HOSTED_PUBLISH_ENABLED` are operator configuration; secrets never enter bundles or git. Unset/false publish-enabled refuses new start/upload with 503 while existing private reads and completed receipt recovery remain available.
Use Netlify platform per-IP limits on public start and agent status routes where supported (initial pilot targets 10 starts/minute/IP and 30 status requests/minute/IP); configure exact route ownership and test effective deploy headers/config. This is delayed best-effort mitigation, not a hard global or account quota. Client five-second polling is pacing, not a security boundary.
No billed provider signup or production deployment is authorized by this plan. Operator must supply two HTTPS origins/sites, exact OAuth registration/callback, test accounts, credentials, pilot traffic/budget envelope and retention responsibility before live acceptance. Public unrestricted signup rollout remains gated until abuse/cost exposure is accepted; no database or billing system is smuggled into v1.
Local integration must exercise real handlers and the packaged CLI with a deterministic OAuth provider fixture and real browser. Live capstone separately proves GitHub callback/scopes, deploy routing/headers, conditional-write race semantics, owner/other-account read isolation and lost-response receipt recovery. Fixture proof cannot close the live capstone.

## Refreshable implementation notes

Read operator setup and actual dependency outcomes before selecting commands. Freeze tested package and deployment revisions in the evidence manifest. A release tarball is acceptable only if the exact artifact is available to a fresh external consumer without repository access and its installation instructions are the public supported path; local `npm pack` alone is not release proof.
Preflight a disposable test artifact containing no confidential source or personal data. Use two operator-approved test identities and record anonymized distinctness plus stable identity assertions; do not print access tokens, cookies, browser fragment secrets or full account API payloads.
No agent may type the user's GitHub password or obtain their session cookie for convenience. Browser sign-in is the user step, followed by the real review approve/deny action.
Prefer authorized test sites and unique fixture titles/operation IDs with a bounded operation count. Name exact cleanup targets before destructive actions. C2 retains completed documents and has no delete API; remove test records only through an approved paused-maintenance operator process after verifying targets, or record their intentional retention. Do not invent a cleanup endpoint.
A dropped upload response can be created at a bounded client-side proxy/interception layer while the real service commits; verify recovery with the same private request file. Live CAS proof must exercise actual Netlify conditional writes, not a fixture ETag map.
Use actual rendered browser output and network/header evidence. A log saying “render sent” cannot prove the user saw the artifact or that a second account was denied.

## Acceptance and verification

AE1 must be driven by Claude following only the installed packaged skill in a clean consumer workspace: ask it to turn selected test material into an Archon doc, let it build/start/checkpoint/resume, and retain offline-readable HTML. A human-authored CLI replay alone does not close R1. Record actual section navigation and theme rendering. Record the tested GitHub account class; do not claim managed-enterprise compatibility unless a separately authorized managed-account test actually passes.

### Agent gate

Before running live steps, produce an exact preflight checklist with each status and supplied authority:

1. Dedicated GitHub OAuth application ID and client secret installed server-side; exact production callback `<HOSTED_APP_ORIGIN>/api/hosted/auth/github/callback` registered, empty requested scopes, correct sign-in origin.
2. Two controlled HTTPS sites on different origins **and different registrable sites**, one app/functions/storage and one static renderer; exact configured origins and no wildcard callback/CORS.
3. Two approved test GitHub accounts usable in isolated browser profiles; neither has to grant repository/org/email scopes to Archon.
4. Deployment credentials/operator coordination, exact revisions, publisher package release/version/integrity and clean external installation path.
5. Accepted pilot traffic/request count/storage/budget envelope, enabled/disabled publication transition authority, and named retention responsibility. The current plan grants none of these external provisioning actions by itself.
6. AHU-012 local integration is passing at the intended release revision, including owner isolation and hostile HTML boundaries.

Absent prerequisites are explicit **blocked** results for the live capstone, not skipped passes. Preparing reviewable runbook/evidence templates can proceed without them.

With prerequisites met and authorization supplied:

- Install the released package in a clean directory, install/read the shipped skill using supported instructions, create benign hosted HTML and start using the real `archon-publish` binary.
- Complete real GitHub sign-in through the user browser. Observe pairing code, descriptor and current account. Verify fixed callback, state/PKCE handling and actual scope outcome without collecting tokens. Approve, resume in a separate client invocation and observe durable receipt plus rendered theme/navigation content.
- Confirm the persisted account key derives from real GitHub numeric identity, and descriptor/owner/HTML in the actual completed record match the receipt/source. Keep evidence content-minimal.
- Open shell, metadata and raw content as owner, signed-out and second account. Inspect private no-store/nosniff/attachment headers and indistinguishable unauthorized/missing responses; no title/bytes appear to non-owners. Check no private artifact is exposed through static site output or public Blobs URLs.
- Demonstrate actual conditional-write winner behavior for parallel identical upload and cancel/upload racing under bounded test traffic; completed owner/bytes never change. Validate ambiguous/lost-response readback and identical receipt recovery after upload deadline within the receipt window, then owner read surviving receipt expiry if the bounded live schedule includes it; otherwise schedule the required follow-up observation and keep that assertion open.
- Drive denied/expired approval and invalid/oversized/mismatched upload cases without accessible partial records. Confirm original local HTML remains available after every failure.
- Load authorized hostile fixture through deployed renderer. Verify exact source/origin handshake, blocked account-origin access and mutation, no tokens/account identity in messages, safe trusted shell, expected CSP/frame headers and failed-renderer behavior. Record the C4 self-navigation/exfiltration limitation honestly.
- Temporarily disable publication only within explicit operator authority; new start/upload refuse 503, existing owner read and completed receipt recovery work, then restore only the agreed setting. Inspect effective deployed rate-limit configuration/headers without claiming a hard global/account quota or generating an unapproved load test.
- Review logs for redaction using test markers; secret values never become report content. Record count/IDs of owned test operations privately and their approved cleanup/retention disposition.

If a new `scripts/test-hosted-live.mjs` is introduced, wire it explicitly into CI in this PR. Untrusted PR CI must test its refusal to perform live actions without required credentials/enablement, or run its safe preflight only; the real operator run is separately gated. Do not put provider credentials in ordinary CI or count a refusal test as live acceptance.
New meaningful tooling assertions need isolated-worktree production-hunk mutation proof where applicable; observational live evidence is not claimed as mutation-tested unit coverage.

### At-merge

The capstone's acceptance report records actual date, exact commands, revisions/package integrity, topology, results and evidence locations, limits, and resource disposition. All required live cases pass, or the ticket remains explicitly open/blocked pending named gates. Tests/runbook source may merge while acceptance remains outstanding; merging those files does not complete this ticket.
No implementation agent marks the whole feature working or live accepted from local tests, HTTP status alone or logs without rendered output.

### Human

The operator personally completes real GitHub sign-in/approval and confirms the owner document rendering, second-account denial and trust/budget boundaries. The final report identifies that human boundary and the actual acceptance evidence without exposing credentials.
User authorization to launch implementation agents is separate from authority to provision/deploy/publish/run live capstone infrastructure.

## Failure, security, migration, and accessibility cases

A GitHub organization restriction or enterprise-managed account failure is an observed limitation with an actionable local artifact fallback, never an invitation to bypass IT controls.
Unexpected OAuth scope, callback/origin mismatch, same-site renderer, public cache leakage, other-account content access, ambiguous owner binding or unsupported provider CAS fails the pilot.
An unavailable provider or missing operator secret is blocked/unavailable, never a successful auth denial or accepted deployment.
Existing root self-hosted deployment, owner seeds and user content remain unchanged. No migration occurs. Exact new test records only are eligible for explicitly approved cleanup.
Verify keyboard approval and reader sign-out, accessible frame title/error, readable artifact theme/fallback fonts and loading/error behavior in real browser output.

## Surfaces

Operator live acceptance runbook, bounded optional runner, dated sanitized evidence report and explicitly gated CI entry if a runner is added. No public marketing claim or rollout toggle is silently changed.

## Sibling boundaries and open gates

All functional behavior comes from AHU-001 through AHU-012. When a live failure requires a production fix, preserve evidence, identify the owning producer and rerun affected real acceptance after its reviewed fix.
Required external gates: OAuth registration/secret, two HTTPS registrable sites, two test accounts, deployment and release authority, accepted budget/traffic envelope, publication enablement and retention owner. Missing one blocks true completion; fixtures cannot substitute.
The current requested planning/preparation phase must stop before implementing agents start. This capstone is future work, not permission to execute live steps during planning.
