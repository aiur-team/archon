# AHU-005 — Isolate interactive artifacts in a static renderer

**Kind:** executable
**Researched at:** dfbd7a7ed2bfb0b2f1742e1d1e2294ab599d1b79
**Requirements:** R2, R10, R11, R14
**Phase:** p2
**Dependencies:** AHU-001
**Complexity:** c3
**Risk:** high — arbitrary active HTML and cross-origin browser isolation

## Outcome

The hosted application can send one authorized HTML artifact to a separate, cookie-free renderer and display real inline interactions without giving artifact code authority over the account page, its storage, or its APIs. The renderer remains a static deployment with no private documents at rest.

## Context and evidence

Baseline evidence is frozen at `dfbd7a7ed2bfb0b2f1742e1d1e2294ab599d1b79`. Refresh the relevant source and already-landed producer interfaces before coding. Names marked **new** below are proposed interfaces, not exports present at that SHA. Frozen contract blocks below take precedence over research sketches; changing them requires a new coordinated planning version.

Neither `renderer/` nor `hosted/` exists at the baseline. Root `netlify.toml` supplies SAMEORIGIN/static cache headers and a legacy edge gate; those settings are incompatible with this new cross-site renderer. `templates/docbuild/src/site.ts` is the legacy site generator, not the owner of this surface. See [hosting research](../research/hosting-security.md), [WHATWG iframe rules](https://html.spec.whatwg.org/multipage/iframe-embed-object.html), [W3C CSP](https://www.w3.org/TR/CSP3/) and [Netlify header behavior](https://docs.netlify.com/manage/routing/headers/) (checked 2026-09-09). Browser-origin enforcement and actual interaction require browser proof; a string containing a sandbox attribute is insufficient.

## Scope

Build the renderer within AHU-001's deployment boundary. Proposed **new** files: `renderer/public/index.html`, `renderer/public/renderer.js` (or a deliberately inline static bootstrap), `renderer/public/renderer.css`, `renderer/scripts/build.mjs`, and `scripts/test-hosted-renderer.mjs`. Own the static security-header generation in the renderer build, including the exact configured app origin and no framing by other sites. AHU-011 invokes that build through its deployment configuration.

Define **new** pure helpers `validateRenderMessage(data)` and `buildArtifactSrcdoc(html)`, plus bootstrap `mountRenderer({ parentWindow, appOrigin, container })` or equivalent small exported seams. These validate the C4 shape and produce the nested frame; they expose no arbitrary RPC. The public runtime receives only the message contract, not these function names. Generate an early restrictive inner policy before any authored active content; preserve the authored HTML as data during assembly. A stricter uploaded CSP may reduce functionality but cannot relax the containing policy.

Use exact target origins and source-window checks for readiness/content. Accept at most one valid artifact per renderer instance. Reject malformed messages, excess bytes, wrong origins and sibling/child windows. A public renderer opened alone displays an inert explanatory state, never a sign-in prompt. Provide a meaningful inner frame title and keyboard-reachable frame boundary. Keep the script/style/resource policy explicit and documented for generation.

## Non-goals

No owner/session/auth API, document fetch, browser-token bridge, content storage, document sharing, collaboration, clipping/clipboard permissions, artifact-driven resizing/navigation, remote asset proxy, sanitization promise, or renderer-origin account cookies. No provider creation or live deployment. Do not modify legacy root headers or builder behavior.

## Existing owner and reuse target

AHU-001 owns the new hosted deployment skeleton and baseline origin/config contract. Consume that origin contract rather than inventing a second policy for trusted origins. Existing `scripts/test-p4-l-browser.mjs` and `scripts/test-p4-k-browser.mjs` show repository browser-test conventions; reuse harness patterns where applicable, with independently configured app/renderer origins. The root test inventory already recognizes tracked `scripts/test-*.mjs`. Do not copy the legacy identity/access modules into this renderer.

## Contract and invariants

## C1. Deployment and identity boundary

The new `hosted/` application is a separate Netlify deployment with its own `package.json`, lockfile, `netlify.toml`, `functions/`, `lib/`, and `public/` tree.
It must not load the legacy all-path edge gate, Netlify Identity, `DOC_OWNERS`, organization defaults, or `PUBLIC_DEFAULT_ROLE`.
The existing root deployment and `scripts/connect.mjs` remain unchanged.
Use Node 22 and pinned `@netlify/blobs@11.0.2`; server imports must resolve in a clean install before any builder compilation.
`renderer/` is a second, static-only deployment on a different origin and registrable site, with no account cookies, credentials, private bytes at rest, or functions.
Production requires HTTPS, exact configured origins, and no wildcard callback or CORS origins.

`HostedPrincipal = { accountId: "gh_" + decimalGitHubId, provider: "github.com", providerUserId: decimalGitHubId, login: string }`.
GitHub numeric ID is canonical; username/email are not ownership keys. Do not request email, repository, organization, or installation permissions.
Dedicated OAuth app, empty scope, fixed GitHub endpoints, server code exchange with client secret, S256 PKCE, single-use state bound to an HttpOnly transient browser cookie.
Reject unexpected granted scopes. Fetch `/user`, validate identity, then discard all GitHub access/refresh tokens.
Browser session is an opaque 256-bit random token; persist its hash with principal and a seven-day absolute expiry.
AHU-003 owns strongly consistent private session records in `archon-hosted-v1` at `sessions/<tokenHash>` and transient auth records under `auth/`. Protected logout and callback rotation durably revoke the old session server-side before claiming success; clearing a cookie alone is insufficient. Revocation uses guarded state updates and ambiguous-write readback, never a public or deploy-scoped store. A revoked token fails on every subsequent identity read.
Transient OAuth-state, pre-login CSRF and publication-binding cookies use distinct `__Host-` names with Secure, HttpOnly, SameSite=Lax, Path=/, no Domain, and at most fifteen-minute TTL. Consume OAuth state once; preserve publication binding through account switching until decision or expiry, then clear it. Do not consume all three cookies on the first bootstrap request.
Cookie: `__Host-archon_session; Secure; HttpOnly; SameSite=Lax; Path=/`; no Domain attribute.
`identifyHosted(request)` returns principal or null; backing-store failure throws an unavailable error, never null.
`requireBrowserMutation(request)` verifies the exact configured Origin and a session-bound CSRF token. The pre-login start form uses a separate transient CSRF binding, not a required authenticated session.
GET never approves publication or logs out. OAuth callback consumes state once and rotates session; redirect destinations are only `/publish/authorize` with server-bound operation or `/docs/<id>`.
The different-account action revokes the Archon session by protected POST, preserves the pending browser binding, then starts a fresh browser-bound GitHub authorization with `prompt=select_account`; do not imply Archon logout logs out GitHub.
Routes: `POST /api/hosted/auth/github/start`, `GET /api/hosted/auth/github/callback`, `GET /api/hosted/session`, `POST /api/hosted/auth/logout`.
Session endpoint returns `{v:1, authenticated:false}` or `{v:1, authenticated:true, accountId, login, csrfToken}`; token is browser-only and must never reach artifact or agent output.

## C4. Private read and isolated rendering

`GET /docs/<id>` is a trusted viewer shell; signed-out requests go to a fixed local sign-in flow with a server-validated ID; other users and missing IDs get indistinguishable not-found views. Never include title/HTML before owner authorization.
`GET /api/hosted/docs/<id>` requires browser owner session and complete state, returns `{v:1,documentId,title,ownerAccountId,contentSha256,contentBytes,createdAt}`.
`GET /api/hosted/docs/<id>/content` repeats owner/complete checks and returns UTF-8 bytes as `application/octet-stream`, `Content-Disposition: attachment; filename="archon-document.html"`, nosniff, private no-store. No public Blobs URL, CDN copy, redirect bearer, or downloadable asset path.
Trusted viewer fetches content same-origin, then sends `{type:"archon:render",v:1,html}` ONLY to configured renderer origin and exact renderer frame window after its `{type:"archon:ready",v:1}` message.
Renderer accepts the message only from exact configured app origin and `event.source === parent`. It places HTML in an inner iframe with `sandbox="allow-scripts"`, never `allow-same-origin`, top navigation, popups, forms, downloads, or credentialless fallback claims.
AHU-005 owns a trusted srcdoc prelude for fragment-only links: prevent native base-URL navigation without stopping authored click listeners, resolve the target and scroll after those listeners, and preserve original href attributes and raw stored bytes. Do not require History API or parent navigation authority on the opaque document; missing targets stay in the current artifact. AHU-012 tests actual packaged navigation and verifies the renderer never replaces the artifact.
The outer renderer is cross-site and cookie-free. If sandboxed it needs `allow-scripts allow-same-origin` for exact origin messaging; it hosts only operator-owned static code. It never receives document URL/ID, session, CSRF, owner data or bearer.
Renderer HTTP CSP bounds scripts/styles/frames; inner srcdoc adds an early policy allowing inline script/style, data/blob image media as needed, and denying connect, forms, object, base and remote subresources. No postMessage from artifact can authorize another fetch or resize/navigate trusted UI.
Sandbox prevents account-origin access, not every possible self-navigation or all content exfiltration by malicious HTML; never advertise network-proof or end-to-end encryption. Use a separate registrable site to keep renderer cross-site for SameSite cookies. Application mutation endpoints still enforce Origin/CSRF independently.
Trusted shell exposes title, owner identity, sign-out, loading, failed renderer and unavailable states; the artifact may not impersonate those regions. Keyboard focus, frame title and screen-reader errors are required.

## C6. Operations and acceptance

`HOSTED_APP_ORIGIN`, `HOSTED_RENDER_ORIGIN`, `GITHUB_CLIENT_ID`, `GITHUB_CLIENT_SECRET`, `HOSTED_PUBLISH_ENABLED` are operator configuration; secrets never enter bundles or git. Unset/false publish-enabled refuses new start/upload with 503 while existing private reads and completed receipt recovery remain available.
Use Netlify platform per-IP limits on public start and agent status routes where supported (initial pilot targets 10 starts/minute/IP and 30 status requests/minute/IP); configure exact route ownership and test effective deploy headers/config. This is delayed best-effort mitigation, not a hard global or account quota. Client five-second polling is pacing, not a security boundary.
No billed provider signup or production deployment is authorized by this plan. Operator must supply two HTTPS origins/sites, exact OAuth registration/callback, test accounts, credentials, pilot traffic/budget envelope and retention responsibility before live acceptance. Public unrestricted signup rollout remains gated until abuse/cost exposure is accepted; no database or billing system is smuggled into v1.
Local integration must exercise real handlers and the packaged CLI with a deterministic OAuth provider fixture and real browser. Live capstone separately proves GitHub callback/scopes, deploy routing/headers, conditional-write race semantics, owner/other-account read isolation and lost-response receipt recovery. Fixture proof cannot close the live capstone.

## Refreshable implementation notes

Start with a browser spike inside the ticket's test fixture: trusted app origin, separate renderer origin, opaque-origin artifact. Show an inline button changing visible DOM, then prove it cannot inspect either parent DOM or account cookies. Resolve CSP inheritance and srcdoc creation in Chromium and a second browser engine before settling headers; do not solve a CSP failure by adding artifact `allow-same-origin`, remote wildcard sources, or weakening the app's CSP.

The static bootstrap may use a tightly scoped inline allowance on the cookie-free renderer to permit authored inline scripts in the nested srcdoc. The account app retains its independent strict policy. HTTP `frame-ancestors` must permit the app origin and reject others; an HTML meta element cannot implement that rule. Avoid inherited SAMEORIGIN on the renderer response. Any build-time origin substitution must parse/validate and escape the value, not concatenate an untrusted URL into script or headers. Input NUL/BOM validation remains AHU-008/AHU-004 ownership, but the renderer still rejects oversized/malformed messages.

## Acceptance and verification

### Agent gate

Run the **new** `node scripts/test-hosted-renderer.mjs` against actual HTTP responses and real browsers using synthetic content. Assert visible inline-control behavior and final computed UI state. Assert artifact reads of parent DOM, cookies and storage fail; attempted credentialed account API requests do not obtain bytes or cause mutations; forged readiness/render messages with correct origin but wrong window, or correct window but wrong origin, do not render or fetch. Assert remote script/image/connect/form/popup/top-navigation attempts do not gain permitted product capabilities. Test an HTML payload containing hostile meta tags, base tags, event handlers, nested frames and closing-tag sequences against the actual bootstrap path.

Exercise keyboard traversal, a useful frame title, standalone renderer state and a denied-message case. Record browser versions and effective response policies. Tests must not contact live GitHub or private stores.

### At-merge

Wire the browser runner as an executing CI step in this producer PR, not merely a comment, and run `node scripts/check-test-inventory.mjs`. Additive edits to `.github/workflows/check.yml` must preserve sibling tests and be reconciled against the latest integration branch. Run the renderer build from a clean checkout and inspect that output contains static code only, no environment secrets/private test fixture content.

For meaningful mutation evidence, add `allow-same-origin` to the artifact frame and show the renderer-parent DOM isolation test fails; remove either origin or source-window validation and show its forged-message test fails. Restore each production change and rerun. Report exact commands and outcomes. A policy-string assertion alone does not close this ticket.

### Human

AHU-013's live capstone verifies the real renderer domain, response headers and supported browser behavior after operator deployment. AHU-012 first reconnects the real local producers. No secret installation or domain purchase is requested by this ticket. Hermetic browser proof does not certify production CSP or DNS.

## Failure, security, migration, and accessibility cases

Renderer load/error/ready timeout must leave a recoverable unavailable state in the consuming app, with no HTML fallback on account origin. Public direct navigation remains empty. Cross-site messaging uses no wildcard target; artifact `postMessage` cannot trigger app actions. Renderer does not receive a document ID, title, owner, cookies or CSRF token. Do not claim that arbitrary malicious HTML is network-proof or cannot consume browser resources.

No legacy documents migrate. A changed renderer protocol requires a versioned coordinated app update; v1 must reject unknown messages instead of guessing. Respect reduced-motion where the trusted shell adds motion, maintain keyboard focus visibility and avoid an endless invisible loading state for assistive technology.

## Surfaces

New static renderer page and its policy/build output; browser message API; compatibility documentation owned here and referenced by AHU-010's generation skill. No new app-auth or storage surface.

## Sibling boundaries and open gates

AHU-009 owns the trusted viewer and content fetch; it consumes exact C4 messages. AHU-011 owns `renderer/netlify.toml`, deployment invocation and operator instructions; this ticket owns bootstrap/header-generation code. Any shared renderer package-lock edit is coordinated with AHU-001's landed skeleton, never replaced wholesale. CI edits are additive shared surfaces: tell the Executor which step was added and preserve all sibling entries on merge.

Operator gates are the configured app/renderer HTTPS origins on different registrable sites. AHU-001 owns public-suffix-aware validation using a pinned suitable library; AHU-011 verifies configured operator inputs. Do not guess the boundary from the last two hostname labels. This ticket freezes one exact browser-tested renderer policy for AHU-009 consumption; do not leave an optional weaker policy variant for consumers. No unresolved gate permits same-origin rendering.
