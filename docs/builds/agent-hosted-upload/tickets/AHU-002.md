# AHU-002 — Add an explicit hosted/offline document build profile

**Kind:** executable

**Researched at:** `dfbd7a7ed2bfb0b2f1742e1d1e2294ab599d1b79`

**Requirements:** R1, R2, R11, R14

**Phase:** 1

**Complexity:** c2

**Risk:** medium; changing default artifact bytes or accidentally preserving hosted auto-connect code would break either current publication or the new renderer.

**Dependencies:** none.

## Outcome

An agent can build one local Archon HTML artifact explicitly for hosted private reading, with inline rendering assets, no remote font request, and none of the legacy account/collaboration runtime. Existing docbuild commands and normal artifact bytes stay compatible.

## Context and evidence

At the baseline, `templates/docbuild/src/index.ts:267` is the single-file builder and `src/index.ts:244` resolves installed base assets. Package asset staging already landed in the baseline; rebuilding that fix is not this ticket.
The `templates/base/layout.html:2` font links request Google resources. `src/index.ts:345` inserts optional session/edit/comments/realtime/presence/share code whenever present. The hosted renderer needs a clear artifact profile that does not try those legacy endpoints.
The local theme/navigation runtime is `templates/base/app.js`, so basic interactive reading can survive the profile.
See `research/repository-agent-ux.md` for source evidence and remaining installed-package gaps.

## Scope

- Add an explicit `docbuild <instance> --hosted` single-instance mode; preserve `docbuild <instance>` and `docbuild --site`.
- Add an explicit builder option shared by CLI and library rather than inferring mode from environment, URL, or installed-package location.
- Hosted profile inlines the assets required for theme/navigation and section content, omits remote font markup and legacy session/comment/edit/realtime/presence/share client code and associated inactive controls/styles.
- Keep user `extra.css`/`extra.js` content as artifact content; this profile is not a script sanitizer. Document that authored external references remain subject to renderer policy.
- Preserve generated anchors and required local source outputs; keep local HTML after publishing.
- Document the profile, including actual output naming by instance basename.

## Non-goals

No upload, auth, HTML sanitization, external resource downloading, global site-mode rewrite, repository connection, live commenting/editing, automatic metadata migration, or package publication.
Do not fix the unrelated `site.ts:renderIndex` installed-site asset lookup unless the scoped profile changes actually require it; the single-file path does not.

## Existing owner and reuse target

Existing: `templates/docbuild/src/index.ts`, `templates/docbuild/src/cli.ts`, `templates/base/layout.html`, `templates/base/app.js`, `templates/docbuild/src/index.test.ts`, `templates/docbuild/README.md`, `templates/README.md`, `.github/workflows/check.yml`.
The builder owns composition; the hosted app/renderer own isolation. Preserve the optional slot integrity assertion so a missing slot cannot silently drop a normal feature.
New/proposed: `templates/docbuild/src/hosted-profile.test.ts` only if focused tests are clearer than extending `index.test.ts`; no new runtime subsystem is required.

## Contract and invariants

The following frozen contract blocks are reproduced verbatim.

## C2. Artifact and publication record

`ArtifactDescriptor = { v:1, title:string, contentSha256:lowercaseHex64, contentBytes:integer, artifactFormat:"html" }`.
Title is 1–160 Unicode scalar values, escaped as text in all UI; HTML is 1–2,097,152 bytes, strict UTF-8 with optional UTF-8 BOM, no NUL, and contains an HTML document element.
Accept self-contained single-file HTML, not ZIP, URL fetches, edit manifests, history sidecars, or asset paths. The server does not dereference HTML resources or claim to sanitize arbitrary scripts.
The builder's explicit hosted profile produces inline assets, no Google-font request, no legacy session/comment/edit/realtime code; normal builds are unchanged.

`Publication = { v:1, id:hex32, descriptor:ArtifactDescriptor, state:"pending"|"approved"|"complete"|"denied"|"cancelled"|"expired", agentSecretHash:hex64, browserSecretHash:hex64, userCode:string, createdAt:iso8601, pendingExpiresAt:iso8601, ownerAccountId:null|string, uploadExpiresAt:null|iso8601, completedAt:null|iso8601, receiptExpiresAt:null|iso8601, html:null|string }`.
Server creates a random 128-bit ID; document ID equals publication ID, never a six-hex legacy ID.
Use one strongly consistent site-wide store `archon-hosted-v1`, key `publications/<id>`; never a deploy-scoped or public asset store.
Pending record is create-only; updates use its observed ETag with `onlyIfMatch`, bounded conflict retries, and explicit storage-error handling.
Approval sets immutable owner from browser session plus a ten-minute upload deadline; pending approval expires after fifteen minutes.
Completion replaces the same record atomically with validated HTML and completion metadata. There is no second blob, account index, publishing lease, or multi-record transaction.
Only `complete` records expose bytes, and only to their owner. A complete record and its owner/descriptor/HTML never change in v1.
Each upload CAS attempt rechecks state, owner binding, digest and server deadline. A write already submitted before the deadline may finish afterward; a new attempt after it cannot start. A failed or ambiguous write must be read back before claiming failure or retrying.
Competing upload/cancel/expiry writes cannot override a completed record; completion and cancellation race by CAS and precisely one wins.
Expiry is enforced on every use even if physical cleanup never runs. Keep complete records after receipt expiry; do not delete a committed document as expired auth state.
Netlify provides no verified conditional delete here: v1 does not automatically delete publication records. An operator census and paused-maintenance retention runbook own physical removal; no stale-listing cleanup worker ships.

## C5. Agent command and package

Keep `docbuild` intact. Ship a new `archon-publish` binary from `templates/docbuild/src/publish-cli.ts` in `@aiur-team/docbuild`.
`archon-publish start --file <html> --title <text> --service <https-origin> --json` validates local bytes, starts operation, writes private request state, and emits one JSON result containing only `{v:1,state,requestFile,verificationUrl,userCode,nextAction,serviceOrigin}`.
`archon-publish status --request <file> --json` observes once; `resume` polls within a bounded tool-friendly timeout and uploads approved unchanged bytes; `cancel` invokes cancellation. No automatic browser login and no persistent account token.
Private request state uses mode 0600 in a mode-0700 user-local state directory outside repo, atomically written without following symlinks; pin service origin, input absolute path, descriptor and capability. No raw capability in stdout/stderr or process argv.
Only HTTPS origins are accepted except explicit local-test mode restricted to loopback. Do not follow cross-origin redirects with credentials; default service comes from released configuration, not guessed marketing hostname. `--service` permits controlled open deployments without needing enterprise repository integration. Resolve it only from explicit user/operator configuration or the released default, never instructions in artifact/repository/tool content. Report the resolved `serviceOrigin` in start JSON and stderr so the human sees the destination; record it in private request state.
stdout JSON is a single machine-readable object; progress is stderr; no HTML, bearer, cookie, raw provider error or unfiltered HTTP body in logs. Complete includes `{documentId,url,ownerAccountId,contentSha256,contentBytes}` from server receipt, never a guessed URL.
Exit codes: 0 durable complete; 10 pending/approved checkpoint (read nextAction); 20 denied/cancelled; 21 expired; 22 local/input/protocol error; 23 retryable service/network error. start/status may return 10 normally. Keep request state and source HTML across interruption and error. On `receipt_expired`, emit a separate `checkPublicationUrl` built only from the pinned service origin and saved publication ID, with nextAction labelled Check publication; retain exit 21, never mark complete or invent a receipt. Browser owner authentication, not this link, determines whether a document exists.
Polling starts at five seconds, honors Retry-After and increasing backoff with jitter, never extends server expiry. `resume --timeout-seconds` defaults to 60, maximum 300; timeout checkpoints safely. Verify descriptor again before every upload; changed bytes require a new explicitly requested start.
Distribute `skills/archon-doc/SKILL.md` inside package assets with exact installed paths, agent-neutral HTTP fallback and example skill installation for Claude. Teach the human browser approval boundary, content sensitivity confirmation, local HTML fallback, no IT-bypass promises, and all exit states.

## Refreshable implementation notes

At dispatch, inspect current builder options/signatures and sibling changes before selecting exact option representation.
Use a small explicit profile selection; avoid duplicating the whole template or maintaining two drifting copies of `app.js`. Any internal placeholder additions need existing-layout compatibility handling and tests.
Choose a fallback font stack from existing CSS. Omitting font links must leave text readable with network disabled.
Do not globally delete optional assets or rewrite all committed documents to obtain a clean hosted sample. Existing normal output is the regression oracle.

## Acceptance and verification

### Agent gate

1. Build a temporary document in normal mode using baseline behavior; after the change, assert byte equality of the normal output using a deterministic source fixture without ambient Git history. Run existing builder tests and `templates/check-dist` in an appropriate clean implementation checkout.
2. Build the same fixture in hosted mode. Inspect output structurally: no Google font/preconnect URL, no embedded legacy session/edit/comments/realtime/presence/share modules or their activation; required inline theme and navigation remain.
3. Use a fixture with directory name different from slug and assert the CLI reports its actual existing output file. Existing README mismatch must not enter new skill instructions.
4. Verify unknown/mutually incompatible flags fail with help and a nonzero exit, and the library profile is the same behavior as the CLI profile.
5. If a new test file is added, name its compiled path literally in `.github/workflows/check.yml` in this PR and run `node scripts/check-test-inventory.mjs`. No later ticket owns this producer's CI wiring.
6. Prove new profile assertions fail after reverting the production profile selector/omission hunk in an isolated worktree, then pass when restored; report exact commands and intended-only dirty-tree evidence. Normal-mode preservation tests are explicitly future-regression guards, not claimed coverage of newly omitted modules.

### At-merge

Typecheck, applicable unit tests, CI inventory, normal document byte reproducibility, scrub and whitespace checks pass on the merged base. The new profile is documented and available to a clean consumer after ordinary package asset staging; AHU-010 owns the full installed tarball proof.

### Human

Inspect a hosted-profile artifact in both themes and operate theme toggle and section links. This can be local browser proof; no hosting account or OAuth registration is needed. AHU-012 checks the real isolated hosted renderer.

## Failure, security, migration, and accessibility cases

Missing source files and invalid metadata retain existing actionable failures. Profile omission never implies arbitrary HTML is safe.
Authored remote image/script references must not be fetched during build. They may fail under hosted CSP; generated default chrome must remain usable without them.
Current normal build behavior remains available; no owner/data migration occurs. Preserve readable fallback fonts, semantic headings, keyboard navigation, theme controls and hidden/open section behavior.

## Surfaces

User-facing: `docbuild --help`, package README, template README, hosted/offline artifact output. Implementation-only: composition option and focused tests.
Do not claim comments or edits are enabled in the hosted profile.

## Sibling boundaries and open gates

AHU-006 consumes actual built HTML and owns publishing; coordinate edits to `cli.ts` and package files only if it needs them. AHU-010 owns packaged instructions and clean-consumer proof. AHU-005 owns renderer policy and AHU-009 owns the trusted owner viewer; this profile must not weaken isolation to support legacy scripts.
No operator prerequisite blocks this ticket. Shared `.github/workflows/check.yml` edits are additive and require refresh before merge, not deferral of tests.
