# AHU-003 — GitHub identity and browser sessions

**Kind:** executable
**Researched at:** dfbd7a7ed2bfb0b2f1742e1d1e2294ab599d1b79
**Requirements:** R3, R4, R6, R7, R8, R9, R10, R11, R15
**Phase:** 2
**Dependencies:** AHU-001
**Complexity:** 3
**Risk:** High: account confusion, OAuth replay, session leakage and fail-open reads.

## Outcome

A visitor signs into the hosted application using GitHub identity without granting repository permissions.
A verified durable GitHub ID becomes the principal of an opaque, revocable Archon browser session.
The browser can discover its current account and obtain a browser-only CSRF token.
Sign-out invalidates the session; malformed credentials and storage outages cannot become authenticated principals.
This ticket establishes authentication consumed by AHU-007 approval and later private document reads.

## Context and evidence

- Baseline netlify/lib/identity.mjs:48 identifies Netlify Identity users, and line 58 uses that service's subject.
- Baseline netlify/functions/login.mjs:183 calls email/password login; it does not implement GitHub sign-in.
- Baseline netlify/lib/access.mjs:550 requires a nonempty owner email and env:DOC_OWNERS provenance.
- Those contracts describe the existing self-hosted application and cannot silently become hosted ownership.
- Read [authentication research](../research/authentication.md), especially A1–A5 and the dated source caveats.
- [GitHub user API](https://docs.github.com/en/rest/users/users) documents durable numeric IDs and optional public email.
- [Current OAuth flow](https://docs.github.com/en/apps/oauth-apps/building-oauth-apps/authorizing-oauth-apps) supports S256 PKCE, server exchange and identity revalidation.
- [August 2026 changes](https://github.blog/changelog/2026-08-14-multiple-redirect-uris-and-token-refresh-for-oauth-apps/) make exact callback settings and expiring token handling material.
- Netlify Identity is supported; the reason for this seam is the hosted identity/session contract, not deprecation.
- Published SDK 2.0.0 writes JavaScript-readable session cookies; do not substitute it for the frozen hosted cookie contract.
- Sources checked 2026-09-09. Refresh provider registration details before implementing; retain the approved external interface.

## Scope

Implement hosted identity validation, OAuth transactions, opaque sessions, browser CSRF and four frozen auth/session routes.
Provide a trusted pre-login form/bootstrap capable of obtaining a transient browser CSRF binding.
That bootstrap must work without an authenticated session and must not rely on GitHub credentials in JavaScript.
A fixed internal return destination may carry a server-bound pending operation or validated document ID.
Add deterministic provider and backing-store seams so all routes can be tested without live GitHub credentials.
Document operator configuration and callback requirements in the hosted application documentation.

## Non-goals

No repository installation, GitHub device flow, email permissions, organization lookup or account linking.
No publication creation, decision or artifact upload handlers.
No legacy Netlify Identity migration or changes to root login/gate behavior.
No persistence of provider access/refresh tokens, account directory or billing system.
No dynamic arbitrary issuer, callback URL or GitHub Enterprise Server support.
No operator OAuth registration, billed signup or production deployment.

## Existing owner and reuse target

AHU-001 owns the hosted deployment scaffold, pinned install and configuration boundary.
Add hosted code beneath that boundary, with no imports escaping to the legacy deploy tree.
Reuse the existing login code's intent to reject malformed destinations and safe generic errors.
Do not import its safeNext unchanged: its broad destination grammar is wider than the frozen hosted destinations.
Reuse Node Web Crypto for random tokens, SHA-256, PKCE and constant-time secret comparisons where applicable.
Pin any additional dependency and include its clean-install lockfile in this PR; avoid adding an auth framework without demonstrated need.
This ticket owns auth-state storage namespaces and adapters; AHU-004 owns publication storage and cannot be a hidden prerequisite.

## Contract and invariants

The following C1 block is reproduced verbatim from contracts.md and is normative for this ticket.

## C1. Deployment and identity boundary

The new `hosted/` application is a separate Netlify deployment with its own `package.json`, lockfile, `netlify.toml`, `functions/`, `lib/`, and `public/` tree.
It must not load the legacy all-path edge gate, Netlify Identity, `DOC_OWNERS`, organization defaults, or `PUBLIC_DEFAULT_ROLE`.
The existing root deployment and `scripts/connect.mjs` remain unchanged.
Use Node 22 and pinned `@netlify/blobs@11.0.2`; server imports must resolve in a clean install before any builder compilation.
`renderer/` is a second, static-only deployment on a different origin and registrable site, with no account cookies, credentials, private bytes at rest, or functions.
Production requires HTTPS, exact configured origins, and no wildcard callback or CORS origins.

`HostedPrincipal = { accountId: "gh_" + decimalGitHubId, provider: "github.com", providerUserId: decimalGitHubId, login: string }`.
GitHub numeric ID is canonical; username/email are not ownership keys. Do not request email, repository, organization, or installation permissions.
Dedicated OAuth app, empty scope, fixed GitHub endpoints, server code exchange with client secret, S256 PKCE, single-use state bound to an HttpOnly transient browser cookie.
Reject unexpected granted scopes. Fetch `/user`, validate identity, then discard all GitHub access/refresh tokens.
Browser session is an opaque 256-bit random token; persist its hash with principal and a seven-day absolute expiry.
AHU-003 owns strongly consistent private session records in `archon-hosted-v1` at `sessions/<tokenHash>` and transient auth records under `auth/`. Protected logout and callback rotation durably revoke the old session server-side before claiming success; clearing a cookie alone is insufficient. Revocation uses guarded state updates and ambiguous-write readback, never a public or deploy-scoped store. A revoked token fails on every subsequent identity read.
Transient OAuth-state, pre-login CSRF and publication-binding cookies use distinct `__Host-` names with Secure, HttpOnly, SameSite=Lax, Path=/, no Domain, and at most fifteen-minute TTL. Consume OAuth state once; preserve publication binding through account switching until decision or expiry, then clear it. Do not consume all three cookies on the first bootstrap request.
Cookie: `__Host-archon_session; Secure; HttpOnly; SameSite=Lax; Path=/`; no Domain attribute.
`identifyHosted(request)` returns principal or null; backing-store failure throws an unavailable error, never null.
`requireBrowserMutation(request)` verifies the exact configured Origin and a session-bound CSRF token. The pre-login start form uses a separate transient CSRF binding, not a required authenticated session.
GET never approves publication or logs out. OAuth callback consumes state once and rotates session; redirect destinations are only `/publish/authorize` with server-bound operation or `/docs/<id>`.
The different-account action revokes the Archon session by protected POST, preserves the pending browser binding, then starts a fresh browser-bound GitHub authorization with `prompt=select_account`; do not imply Archon logout logs out GitHub.
Routes: `POST /api/hosted/auth/github/start`, `GET /api/hosted/auth/github/callback`, `GET /api/hosted/session`, `POST /api/hosted/auth/logout`.
Session endpoint returns `{v:1, authenticated:false}` or `{v:1, authenticated:true, accountId, login, csrfToken}`; token is browser-only and must never reach artifact or agent output.

## Refreshable implementation notes

Proposed owned production paths:

- hosted/lib/identity.mjs: identifyHosted and requireBrowserMutation, principal parsing, session/CSRF boundary.
- hosted/lib/auth-store.mjs: strong reads, guarded auth-transaction consumption and session revocation.
- hosted/lib/github-oauth.mjs: fixed provider URL construction, PKCE and token/user exchange.
- hosted/functions/auth-github-start.mjs and auth-github-callback.mjs: method-specific handlers.
- hosted/functions/session.mjs and auth-logout.mjs: the frozen session/logout routes.
- hosted/public/login/index.html and hosted/public/login/login.js: minimal trusted sign-in form/bootstrap.
- hosted/lib/identity.test.mjs, auth-store.test.mjs and github-oauth.test.mjs.
- hosted/functions/auth.test.mjs: HTTP handlers exercised with injected provider/store/clock.
- hosted/test/fixtures/auth.mjs: immutable fake principals and narrow documented adapter examples for downstream consumers.

Confirm scaffold naming on AHU-001's merged branch before edits; coordinate renames without changing route contracts.
Expose production factories or narrow injected dependencies, not environment-controlled production authentication bypasses.
Define stable safe error types so AHU-007 can distinguish absent session from unavailable backing state.
Store only the minimum browser-transaction metadata necessary for redirect binding.
A pending operation is opaque here: this ticket validates/binds transport context but never reads or changes its owner.
For single-use state, use a consumed marker with ETag or another documented atomic primitive; an unconditional delete is insufficient.
No scheduled transaction/session cleanup is required; expiry must be enforced on lookup.
Document bounded retention and operator maintenance instead of assuming Blobs TTL or conditional delete support.
Expose the transient binding operation AHU-007 needs via an exported helper and immutable test fixture.
Record its exact signature in hosted auth documentation before this producer merges.
The session endpoint is the browser's source of account identity; do not trust an agent-supplied account hint.

## Acceptance and verification

Prove server-side logout and callback rotation reject a copied old token, including revocation-store failure. Check all transient cookie attributes and one-use state independently. The account-switch browser test retains an existing GitHub provider session and verifies explicit account selection rather than assuming Archon logout logs GitHub out.

### Agent gate

Run each named test explicitly from the isolated ticket worktree after implementing it.
Add literal paths for every owned test to .github/workflows/check.yml in this PR.
Required commands include node --test hosted/lib/identity.test.mjs hosted/lib/auth-store.test.mjs hosted/lib/github-oauth.test.mjs hosted/functions/auth.test.mjs.
Run the AHU-001 hosted module-load check after hosted npm ci and before any docbuild compilation.
Run node scripts/check-test-inventory.mjs; a test inventory omission blocks this ticket.
Use a deterministic OAuth fixture and injected time/entropy/store failures; no real user credentials or home-state reads.
Assert actual principal values and revocation effects, not merely rendered sign-in text.
Test two simultaneous callbacks with the same state: at most one creates a session.
Test the same numeric ID with different login/email snapshots and different IDs sharing a login/email fixture.
Prove absent public email succeeds and malformed/unsafe provider IDs cannot collide with valid account IDs.
Test missing/mismatched/replayed state, bad PKCE, missing binding cookie, wrong Origin and forged CSRF.
Test callback redirect injection including //host, encoded separators, backslashes and unknown internal destinations.
Test unexpected scope response, denied consent, malformed provider JSON and provider/user API timeout.
Test backing-store outage from identifyHosted throws unavailable while an absent/expired/revoked session returns null.
Test session endpoint exact response shape and absence of raw provider token, cookie value and storage hashes.
Test logout GET does not revoke; forged POST cannot revoke; valid POST makes the prior cookie unusable.

### At-merge

All owned tests, hosted clean import check, existing root gates and inventory checker pass on the ticket head.
Verify cookies through actual HTTP responses: HttpOnly, Secure, host prefix, SameSite and no Domain.
For every newly added test, remove the specific production guard it protects in an isolated clean worktree and confirm failure.
Mutation examples: use login as accountId, allow null email to fail, return null on store exception, skip state consumption, remove Origin check.
Restore each production hunk and rerun; include exact commands and per-test failure evidence in the PR body.
Do not count already-green baseline behavior as coverage of a new guard.
Missing secrets cannot skip deterministic route verification.
Publish adapter fixtures and consumer documentation in this PR so AHU-007 can merge against a real producer.

### Human

The operator later supplies the dedicated OAuth registration and two controlled GitHub accounts.
Live acceptance must verify displayed scopes, exact callback behavior and owner identity after account switching.
This ticket can merge with deterministic evidence; it cannot claim live GitHub sign-in was verified without that session.
The final capstone owns integrated upload/read proof and does not replace these ticket-local tests.

## Failure, security, migration, and accessibility cases

Auth provider outage and storage outage have distinct internal causes but safe unavailable client behavior.
Never include raw provider response bodies, authorization codes, CSRF, cookie values or secrets in logs.
Reject invalid configuration before serving auth mutations; do not infer a callback from marketing hostname.
A login retry starts a new browser-bound transaction without replaying a consumed callback.
Unexpected refresh fields from modern GitHub responses are discarded, not persisted as a new capability.
Session absolute expiry is checked from server time even when physical records remain.
Logout clears the browser cookie but must also revoke server authority; a copied old cookie must fail.
Pre-login sign-in controls are keyboard reachable, labelled and have an announced error state.
A network failure leaves an actionable retry path without selecting a different account silently.
There is no migration of existing sessions or ownership records; hosted principals cannot authorize legacy routes.
Verify no hosted bundle imports @netlify/identity and no root deployment file changes are needed.

## Surfaces

New hosted auth APIs, sign-in page, cookies, identity/session adapter and operator configuration documentation.
Potential additive edits: hosted/package.json, hosted/package-lock.json, hosted/netlify.toml and .github/workflows/check.yml.
No new repository-facing GitHub API surface; only fixed OAuth endpoints and authenticated /user are allowed.

## Sibling boundaries and open gates

AHU-001 must merge the clean hosted scaffold first.
AHU-004 may run in parallel; neither ticket imports the other's unfinished code.
AHU-007 consumes this identity/CSRF/transient-binding seam and owns pending approval UI/HTTP.
AHU-008 consumes publication state and does not accept this ticket's browser session as agent authority.
Later private-read ticket consumes identifyHosted without implementing another principal rule.
Coordinate additive CI changes; preserve all sibling literal test entries during merge.
Do not weaken C1 to accommodate a library or unconfigured provider; report the concrete incompatibility to Executor.
OAuth registration, HTTPS origins and live accounts remain operator-owned capstone gates.
